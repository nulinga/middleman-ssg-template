{
  "devDependencies": {
    "esbuild": "^0.15.8",
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "build:esbuild": "esbuild source/javascripts/*.js --outdir=dist/javascripts/ --bundle --minify",
    "watch:esbuild": "esbuild source/javascripts/*.js --outdir=dist/javascripts/ --bundle --watch"
  }
}
